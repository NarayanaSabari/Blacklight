# =============================================================================
# Inngest Server - Environment Configuration
# =============================================================================
# Copy this file to .env.inngest and configure the values
# Usage: docker-compose -f docker-compose.inngest.yml --env-file .env.inngest up -d
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL (for Inngest state storage)
# -----------------------------------------------------------------------------
POSTGRES_USER=inngest
POSTGRES_PASSWORD=change_this_to_a_secure_password
POSTGRES_DB=inngest

# -----------------------------------------------------------------------------
# Inngest Keys
# -----------------------------------------------------------------------------
# CRITICAL: These MUST match the values in your Cloud Run backend!
# Generate with: openssl rand -hex 32
#
INNGEST_EVENT_KEY=deadbeef0123456789abcdef01234567
INNGEST_SIGNING_KEY=cafebabe0123456789abcdef01234567

# -----------------------------------------------------------------------------
# Backend URL (Your Cloud Run backend)
# -----------------------------------------------------------------------------
# This is the PUBLIC URL of your Cloud Run backend service
# Inngest will call this URL to discover functions and report results
#
BACKEND_URL=https://blacklight-backend-52414647599.asia-south1.run.app
