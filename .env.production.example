# =============================================================================
# Blacklight Production Environment Variables
# Copy this file to .env.production and fill in your actual values
# =============================================================================

# =============================================================================
# GENERAL CONFIGURATION
# =============================================================================
ENVIRONMENT=production
LOG_LEVEL=INFO

# =============================================================================
# SECURITY
# =============================================================================
# IMPORTANT: Generate a strong secret key for production
# You can generate one using: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_TO_STRONG_RANDOM_SECRET_KEY

# Allowed hosts (comma-separated, no spaces)
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# CORS Origins (comma-separated, no spaces, or use * for development only)
CORS_ORIGINS=https://your-domain.com

# =============================================================================
# DATABASE - PostgreSQL
# =============================================================================
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# =============================================================================
# GOOGLE GEMINI AI
# =============================================================================
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# Model configuration
GEMINI_MODEL=gemini-2.5-flash
GEMINI_EMBEDDING_MODEL=gemini-embedding-001
GEMINI_EMBEDDING_DIMENSION=768

# =============================================================================
# GOOGLE CLOUD STORAGE
# =============================================================================
# Your GCS configuration
STORAGE_BACKEND=gcs
GCS_BUCKET_NAME=your-bucket-name
GCS_PROJECT_ID=your-project-id

# NOTE: Place your GCS credentials JSON file at: server/gcs-credentials.json

# =============================================================================
# SMTP EMAIL CONFIGURATION
# =============================================================================
SMTP_ENABLED=true

# For Gmail:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password": https://myaccount.google.com/apppasswords
# 3. Use the app password below
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME=Blacklight HR

# For SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_USE_TLS=true

# =============================================================================
# INNGEST BACKGROUND JOBS
# =============================================================================
# Get your keys from: https://www.inngest.com/
# Or leave empty to use local dev mode
INNGEST_EVENT_KEY=your-inngest-event-key
INNGEST_SIGNING_KEY=your-inngest-signing-key

# =============================================================================
# FRONTEND URLS
# =============================================================================
# Update these to your actual domain or keep as-is for local/VM deployment
FRONTEND_BASE_URL=https://your-domain.com
FRONTEND_URL=https://your-domain.com/portal
CENTRALD_URL=https://your-domain.com/centrald

# Frontend build-time environment variables
VITE_PORTAL_API_URL=/api
VITE_PORTAL_APP_NAME=Blacklight Portal

VITE_CENTRALD_API_URL=/api
VITE_CENTRALD_APP_NAME=Blacklight CentralD

# =============================================================================
# OPTIONAL TOOLS (pgAdmin, Redis Commander)
# =============================================================================
PGADMIN_EMAIL=admin@blacklight.com
PGADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# 1. NEVER commit this file to git with real credentials
# 2. Use strong, unique passwords for all services
# 3. Update ALLOWED_HOSTS and CORS_ORIGINS to your actual domain
# 4. Place your GCS credentials JSON at: server/gcs-credentials.json
# 5. For SSL certificates, place them in nginx/ssl/ or use Let's Encrypt
